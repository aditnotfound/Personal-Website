// @ts-check
import { defineConfig } from 'astro/config';

// Custom domain: https://adit.email
// Hosted on GitHub Pages via public/CNAME.
export default defineConfig({
	site: 'https://adit.email',
	base: '/',
});
